class Animal {

    int age;
    float taille;
    float poids;

    void set(int a, float t, float p)
    {
        this.age = a;
        this.taille = t;
        this.poids = p;
    }

    void affiche() {
        println("Age :", this.age);
        println("Taille :", this.taille);
        println("Poids :", this.poids);
    }

    void crier(){
        
    }
}