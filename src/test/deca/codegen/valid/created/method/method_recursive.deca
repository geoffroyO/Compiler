// Description:
//      Utilisation d'une méthode récursive
// Resultats:
//      120

class A {
    int fact(int n)
    {
        if (n == 0 || n == 1)
        {
            return(1);
        }
        return(fact(n-1)*n);
    }
}

{
    A a = new A();
    println(a.fact(5));
}
