// Description :
// Test tous les types d'opérations binaire sur des booleens
//
// Resultats :
// OK (Resultat dans synt)

{
    boolean a = true;
    boolean b = !true;

    boolean d, e, f, g, i;

    int c = 2;
    d = (5 == 6789);
    d = (5.0 == -567.3);
    d = (5.0 == 6567);
    // (eq)

    e = (9.3 != 98);
    e = (9.3 != 9.3);
    e = (9.3 != 2);
    // (neq)

    f = (5 < 87);
    f = (5 < 5);
    f = (5 < 5.9);
    // (lt)

    g = (8.5 > 75);
    g = (5 > 5);
    g = (5 > 5.9);
    // (gt)

    f = (5 <= 7);
    f = (5 <= 5);
    f = (5 <= 5.9);
    // (leq)

    g = (56.7 >= 12);
    g = (5 >= 5);
    g = (5 >= 5.9);
    // (geq)

    i = a && b;
    // (and)
    
    i = a || b;
    // (or)
    
    i = a == b;
    // (eq)

    i = a != b;
    // (neq)
}
